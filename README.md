# 🎄 Бот для дропшиперів — ялинки та новорічний декор

Всі файли лежать пласко (без папок) — завантажуються на GitHub простим перетягуванням.

## Запуск
1. Створіть бота у @BotFather, збережіть токен
2. `python setup_np.py ВАШ_КЛЮЧ_НП "Ваше місто"` — видасть 6 рядків для змінних
3. Google Таблиця замовлень: Розширення → Apps Script → вставте `orders_webhook.gs` → Деплой як веб-додаток (доступ: усі) → URL = SHEET_WEBHOOK_URL; у Налаштуваннях проєкту → Властивості скрипту додайте SHEETS_API_SECRET (довгий випадковий рядок)
4. Railway: Deploy from GitHub repo → Variables:
   - BOT_TOKEN, ADMIN_IDS
   - NP_API_KEY, NP_SENDER_REF, NP_SENDER_CONTACT_REF, NP_SENDER_CITY_REF, NP_SENDER_WAREHOUSE_REF, NP_SENDER_PHONE
   - SHEET_WEBHOOK_URL, SHEETS_API_SECRET (те саме значення, що у Властивостях скрипту)
   - опційно: PRICE_TIER (drop1/drop2/drop3), NP_PAYER_TYPE (Recipient/Sender), ADMIN_CHAT_ID, WEBLIUM_SECRET

## Команди адміна
/users — список дропшиперів, /block ID — закрити доступ, /reload — оновити ціни з прайсу, /id — показати ID чату, /np_sync — перевірити статуси ТТН зараз

## Часовий пояс
У Railway має бути змінна `TZ=Europe/Kyiv`, і той самий пояс — у таблиці
(Файл → Налаштування → Часовий пояс) та в Apps Script. Перевірка: функція `checkTimezones()`
в редакторі скрипта показує пояс скрипта й таблиці та час, який має бути в боті.

Бот не покладається на `TZ`: дати ставляться через `config.now()` у поясі `config.TIMEZONE`
(типово Europe/Kyiv), а скрипт віддає дату створення зі зсувом (`+03:00`). Тож навіть якщо
змінна зникне при перезбірці контейнера, вік замовлення й дата ТТН лишаться правильними.

## Тариф дропшипера
В аркуші «Дропшипери» є колонка **«Тариф»**: `drop1` / `drop2` / `drop3` (можна писати «Дроп 2»).
Порожньо — діє загальний `PRICE_TIER` із змінних. Бот колонку лише читає й не затирає,
коли оновлює рядок дропшипера.

## Залишки дропшиперів
Аркуш «Залишки дропшиперів» ведеться руками, бот його лише читає. Колонка «Дроп-ціна» —
довідкова: ціну бот завжди бере з прайсу за тарифом. При старті бот звіряє артикули залишків
із каталогом і пише в лог ті, що не знайшлися (часта причина — вкладки прайсу немає
в `PRICELIST_TABS`, лікується `/reload` або `/gids`).

Артикули в прайсі подекуди набрані з кириличними літерами («Cr6сustom-220» з кириличною с).
Для пошуку бот зводить артикул до канонічного вигляду (`article_key.py`), але зберігає й друкує
в ТТН **оригінальний** рядок із прайсу.

## Залишки зі складської таблиці
Склад — файл у Drive (`WAREHOUSE_SHEET_ID`), прайс — приймач. Тип складу бот визначає сам
за mimeType: **.xlsx** качається через `files().get_media()` і читається openpyxl, рідна
Google-таблиця — через Sheets API. Розрізняти обов'язково: Google показує .xlsx у редакторі
Таблиць і навіть віддає CSV-експорт, але Sheets API на такий файл відповідає
400 «The document must not be an Office file». Службовому акаунту потрібні права
на Sheets і на Drive (читання). Автоматичне перенесення типово **вимкнене** (`WAREHOUSE_SYNC_SECONDS=0`);
вручну — `/sync_stock`. Увімкнути цикл: `WAREHOUSE_SYNC_SECONDS=3600`.

Синхронізуються всі вкладки прайсу, де знайдеться колонка залишку (зараз це «PE Umbrella
System» — колонка «наявності», і «Mini Tree» — «наявність»); решта пропускається.
Артикули зводяться канонічним ключем плюс кількома явними правилами (`warehouse_sync.py`):
літера-суфікс переїжджає перед розмір (`Cr3-150F` → `Cr3F-150`), Люкс складу = Преміум прайсу
(`Cr3-150L` → `Cr3P-150`), останнє слово переїжджає перед розмір
(`Cr7lush-240 mіx` → `Cr7lush mіx-240`), і таблиця назв моделей `MODEL_ALIASES`
(`Cr6wide` складу = `Cr6G` прайсу). Останнє правило сильніше за точний збіг, але відступає,
якщо на складі з’явиться власний рядок із таким артикулом. Усе інше без пари не вгадується,
а перелічується у звіті.

Пишемо **тільки** в колонку залишку, знайдену за заголовком, і лише в тих вкладках, що
перелічені у `WAREHOUSE_TABS`. Колонки цін, артикулів і розмірів не чіпаються ніколи.
Заголовок звіряється повним текстом, а не підрядком: у прайсі є цінова колонка
«Дроп 3 — товар у наявності на складі», і пошук за словом «склад» записав би залишки в ціни.

Число → кількість (нуль теж пишеться: «немає» — це факт), «так/є/+» → «є», **порожня
клітинка складу → позицію не чіпаємо взагалі**. Артикул, який зник зі складу, очищається.
Над заголовком залишку пишеться рядок «Склад від … · синхр. …».

У колонці артикулів складу є підписи блоків («New York», «КОЛОБОК», «звичайна густа») — вони
стоять ПІСЛЯ свого блоку, і той самий артикул буває в кількох блоках. Блок «КОЛОБОК»
відкидається за назвою підпису, а не за номерами рядків: `SKIP_BLOCK_LABELS`.

Синхронізація ВІДМОВЛЯЄТЬСЯ (і прайс лишається як був), якщо: весь стовпець залишку в
джерелі порожній (ознака відсутнього кешу формул, а не розпродажу), немає заголовка,
немає доступу до файла або файл видалено.

## Порядок моделей для дропшипера
Моделі, на які в дропшипера є передзамовлення (рядок в «Залишках дропшиперів»), показуються
першими в списку категорії й позначені ⭐. Решта йде далі у звичайному порядку прайсу.
Дропшипери без передзамовлення бачать список як раніше.

Порядок задає одна функція `catalog.models_for(user_id, category)` — нею користуються і
клавіатура, і обробник вибору моделі, і список розмірів. Якщо десь узяти `catalog.models()`
напряму, номер моделі в кнопці вкаже не на ту позицію.

## Кілька товарів в одному замовленні
Після вибору кількості товар потрапляє в кошик, а не одразу на оформлення: можна додати ще
позиції, прибрати зайві й лише тоді перейти до отримувача. Отримувач, оплата й доставка —
одні на все замовлення.

**На все замовлення одна накладна**: вага, обʼєм і вартість — суми позицій, а місць у
відправленні стільки, скільки одиниць товару (дві ялинки — два місця). У таблиці — рядок на
позицію зі спільним номером замовлення; ТТН, отримувач, оплата й доставка в рядках
повторюються.

Опис накладної при кількох позиціях — перелік артикулів: «Штучні ялинки: Cr6G-220 ×1,
Cr3P-210 ×2». Якщо категорії різні — «Новорічний декор». Ліміт НП 100 символів: список
ріжеться по межі позиції з хвостом «+N поз.», артикул не рветься посередині.

Резерв робиться одним запитом `reserve_many` на всі позиції: скрипт під одним замком перевіряє
всі й пише, лише якщо вистачає кожної. Не вистачило — у таблиці не змінилось нічого, накладних
не створювали.

Якщо накладну створити не вдалося, замовлення однаково записується (статус «ТТН НЕ створено»,
причина в коментарі), резерв лишається, а адмін отримує повідомлення й оформлює накладну вручну.

## Резерв залишків і статуси Нової Пошти
Бот резервує товар у «Залишках дропшиперів» **до** створення ТТН: не вистачає — замовлення не
оформлюється й накладна не створюється. Колонки: G «Зарезервовано», H «Отримано», J «Оновлено».
Колонку I «Доступно» (формула `=F-G-H`) не пише ніхто — перевірити формули можна функцією
`checkAvailableFormulas()` у редакторі Apps Script.

Раз на `TTN_POLL_SECONDS` (типово година, `0` — вимкнути) бот перепитує статуси ТТН у НП і реагує
**на зміну** статусу, не на сам статус:
* «отримано» → резерв стає видачею (G → H);
* «відмова/повернення» → лише повідомлення адміну, резерв лишається (рішення за людиною);
* «видалено» (код 2) → резерв знімається одразу;
* «номер не знайдено» (код 3) → резерв НЕ знімається одразу: НП так відповідає й на щойно
  створену накладну. Перший раз — лише лист адміну, резерв стоїть. Знімаємо, тільки якщо код 3
  повторився на наступній перевірці І замовленню понад добу; у статус дописується
  «· резерв знято», щоб не зняти його вдруге.

## Дублювання замовлень у таблицю дропшипера
У Властивостях скрипту: `DROPSHIPPER_EXPORT_SHEETS_JSON` = `{"Telegram ID":"ID таблиці"}`.
Обліковий запис, під яким працює скрипт, має бути редактором тієї таблиці. Збій експорту
не зриває замовлення — воно однаково потрапляє в головну таблицю. Разово перенести старі
замовлення: функція `syncDropshipperOrdersFromAll()` у редакторі.

## Оновлення цін
Прайс має бути доступний «усім за посиланням» → команда /reload у боті.

## Вебхук Weblium
POST на https://ВАШ-ДОМЕН/weblium?secret=WEBLIUM_SECRET з полями article/product, qty, payment, fio, phone, city, warehouse.
